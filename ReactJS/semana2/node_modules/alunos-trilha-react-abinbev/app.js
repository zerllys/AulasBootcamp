const alunos = document.querySelector("#alunos")

fetch("http://localhost:3000/alunos")
  .then(resposta => resposta.json())
  .then(dados => showAlunos(dados))
  

function showAlunos(dados) {
  let lista = ''
  dados.map(aluno => lista += `<li>${aluno}</li>`)

  alunos.innerHTML = lista
}